{
  "name": "chibi-in-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --config vite.content.config.js",
    "build:content": "vite build --config vite.content.config.js",
    "package:xpi": "powershell -Command \"Compress-Archive -Path manifest.json, popup, dist, source, textures, src -DestinationPath chibi-goobers.zip -Force; Move-Item -Path chibi-goobers.zip -Destination chibi-goobers.xpi -Force; Write-Host 'Packaged chibi-goobers.xpi successfully!'\""
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
